.env
.env.*
!.env.example
.study-data/
.venv/
venv/
__pycache__/
*.pyc
*.sqlite3
*.sqlite3-wal
*.sqlite3-shm
node_modules/
dist/
Asset/
# railway up packs its upload context from git-visible files, so builtin_books
# (copyrighted, never pushed) must stay UNTRACKED but NOT ignored: they ride
# the build context as plain untracked files, guarded by a local pre-commit
# hook — see the deploy notes in README.
