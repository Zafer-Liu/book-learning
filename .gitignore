.env
.env.*
!.env.example
.study-data/
.venv/
venv/
__pycache__/
*.pyc
*.sqlite3
*.sqlite3-wal
*.sqlite3-shm
node_modules/
dist/
Asset/
# Railway CLI packs its upload context following .gitignore, so paths that
# must stay out of git but inside the Docker build context (builtin_books,
# .railway) live in .git/info/exclude instead — see README deploy notes.
